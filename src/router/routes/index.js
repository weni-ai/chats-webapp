import orgs from './orgs';
import chats from './chats';
import dashboard from './dashboard';
import settings from './settings';
import connect from './connect';

export default [...orgs, ...chats, ...dashboard, ...settings, ...connect];
